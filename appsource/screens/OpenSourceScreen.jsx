import React from 'react';
import { Flex, ScrollView } from 'native-base';
import { Button, Card, Title, Text, Surface } from 'react-native-paper';

const OpenSourceScreen = (props) => {
    return (
        <ScrollView>
            <Surface></Surface>
        </ScrollView>
    );
}

export default OpenSourceScreen;